# Методы

## count

Считает число вхождений элемента в кортеж

```python
t = ('a', 'b', 'c', 'b')
t.count('b')  # 2
```

## index

Находит элемент по его значению и возвращает его индекс

```python
t = ('a', 'b', 'c', 'd')
t.index('b')  # 1
```
