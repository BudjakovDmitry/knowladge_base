# title

Элемент `<title>...</title>` содержит заголовок страницы. Данный элемнет располагается внутри элемента `head`.

```html
<html>
    <head>
        <title>My page</title>
    </head>
</html>
```
